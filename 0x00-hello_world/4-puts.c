#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	/* String initialization */
	char Mystr[] = "\"Programming is like building a multilingual puzzle";

	puts(Mystr); /* Writes the string to stdout */

	return (0);
}

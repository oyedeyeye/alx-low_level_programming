#include "main.h"

/**
 * main - tests the functions that prints if integer is positive or negative
 * Description: Learning to debug by calling user defined function
 * Return: ALways 0 (Success)
 */
int main(void)
{
	int i;

	i = 98 % 2;
	positive_or_negative(i);

	return (0);
}

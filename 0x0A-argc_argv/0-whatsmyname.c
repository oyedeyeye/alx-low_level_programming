#include <stdio.h>

/**
 * main - Entry point for applicaton
 * @argc: Size of cmd arguments
 * @argv: Array of cmd arguments
 * Return: sucess - 0
 */
int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
